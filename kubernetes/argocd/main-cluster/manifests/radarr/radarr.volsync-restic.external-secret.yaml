apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-restic-config
  namespace: radarr
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: main-secret-store
    kind: ClusterSecretStore
  target:
    name: radarr-restic-config
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        RESTIC_REPOSITORY: rest:http://restic.moonbase.home/radarr-config
        RESTIC_PASSWORD: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: restic/main-backups/password
        version: v1
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
