apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lidarr-restic-config
  namespace: lidarr
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: main-secret-store
    kind: ClusterSecretStore
  target:
    name: lidarr-restic-config
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        RESTIC_REPOSITORY: rest:http://restic.moonbase.home/lidarr-config
        RESTIC_PASSWORD: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: restic/main-backups/password
        version: v1
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
